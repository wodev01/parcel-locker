import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PickupFromSlotPage } from './pickup-from-slot';

@NgModule({
  declarations: [
    PickupFromSlotPage,
  ],
  imports: [
    IonicPageModule.forChild(PickupFromSlotPage),
  ],
})
export class PickupFromSlotPageModule {}
